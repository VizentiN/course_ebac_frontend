const multiplicacao = (num1: number, num2: number): number => num1 * num2
const dizHi = (nome:string): string => `${nome} está dizendo Hi!`

console.log(multiplicacao(5,5))
console.log(dizHi('Lucas'))